<?php

/**
 * English (default) translations for sugar-stickers.
 *
 * @return array<string, string>
 */

declare(strict_types=1);

return [
    // sugar-stickers is a purely computational layout library (FlexBox, Table).
    // No user-facing natural-language strings are rendered at runtime.
    // Translation keys are declared here for infrastructure completeness
    // and future use if label-rendering methods are added.
];
